
// 3-coloring protocol
// We want no two adjacent colors to be the same.

constant N := 5;

variable c[Int % N] <- Int % 3;

process P[i <- Int % N]
{
  read: c[i-1];
  write: c[i];
  read: c[i+1];
  (future & silent)
    (c[i-1] != c[i] && c[i] != c[i+1]);
}

