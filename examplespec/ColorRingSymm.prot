
// 3-coloring protocol
// We want no two adjacent colors to be the same.

constant N := 3;

variable x[Int % N] <- Int % 3;

process P[i <- Int % N]
{
  symmetric j <- {# i-1, i+1 #}
  {
    read: x[j];
  }
  write: x[i];

  (future & silent)
    (x[i-1]!=x[i] && x[i]!=x[i+1]);
}

