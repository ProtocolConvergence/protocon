
constant N := 3;

constant M := 7;
constant R := 3;

variable x[Nat % N] <- Nat % M;

process P[i <- Nat % N]
{
  read:  x[i-1];
  write: x[i];

  (future & silent)
    ((x[i-1]^2 + x[i]^3) % M == R);
}

