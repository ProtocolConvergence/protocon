/**
 * \file main.hh
 * This file only exists so the Makefile can have a rule:
 *   %.o: %.cc %.hh
 * That is, all .cc files have an associated .hh file.
 * There is a /proper/ way to do this by generating dependencies or using
 * more sophisticated system like CMake.
 **/
#ifndef MAIN_HH_
#define MAIN_HH_
#endif

